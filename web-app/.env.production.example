# Bu deger web-app/.env DEGIL - Vercel projesinin kendi Environment
# Variables ekranina (Project Settings > Environment Variables) girilir.
# Buraya sadece referans/hatirlatma icin birakildi.
#
# DOMAIN, docker-compose.prod.yml / .env dosyandaki DOMAIN ile ayni
# olmali (Caddy'nin HTTPS sertifikasi verdigi VPS adresin).
VITE_API_BASE_URL=https://api.senin-alan-adin.com/api
